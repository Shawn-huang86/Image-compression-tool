{
  "name": "image-compression-tool",
  "version": "1.0.0",
  "description": "Pure frontend image compression tool with batch processing",
  "main": "index.html",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "jszip": "^3.10.1",
    "heic2any": "^0.0.4"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  },
  "keywords": ["image", "compression", "frontend", "typescript"],
  "author": "",
  "license": "MIT"
}